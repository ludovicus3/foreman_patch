<% stylesheet 'foreman_patch/foreman_patch' %>

<% title _("Edit %s") % @plan.name %>

<%= form_for @plan do |f| %>
  <%= base_errors_for @plan %>
  <%= render 'fields', f: f %>

  <%= submit_or_cancel f, false, {cancel_path: plans_path} %>
<% end %>
