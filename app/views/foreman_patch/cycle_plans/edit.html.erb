<% stylesheet 'foreman_patch/foreman_patch' %>

<% title _("Edit %s") % @cycle_plan.name %>

<%= form_for @cycle_plan do |f| %>
  <%= base_errors_for @cycle_plan %>
  <%= render 'fields', f: f %>

  <%= submit_or_cancel f, false, {cancel_path: cycle_plans_path} %>
<% end %>
