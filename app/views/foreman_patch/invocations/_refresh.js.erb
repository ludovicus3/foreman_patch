<% if @auto_refresh %>
$(function() {
  delayed_refresh($("div.terminal#<%= @phase %>").data('refresh-url'),
    {"since": "<%= @line_sets.last.try(:[], 'timestamp') || @since %>", "line_counter": $("div.terminal#<%= @phase %> div.line").size()});
});
<% end %>

