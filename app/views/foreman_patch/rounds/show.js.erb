//$('div#title_action div.btn-group').html(' button_group(round_task_buttons(@round.task)).html_safe ');

<% if @auto_refresh %>
  delayed_refresh($('script#round_invocation_refresh').data('refresh-url'), round_invocation_refresh_data());
<% end %>
