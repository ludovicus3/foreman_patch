
export const prioritizeGroups = (groups) => {
  return groups.reduce(
    (priorities, group) => {
      let priority = group.priority;
      
    }, new Array())
    .filter(
      item => (item != null)
    );
};
