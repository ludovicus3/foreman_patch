import React from 'react';
import { translate as __ } from 'foremanReact/common/I18n';
import { Table, TableHeader, TableBody, TableComposable } from '@patternfly/react-table';

const MainTable = ({

}) => {
    return (
        <Table>
            <TableHeader />
            <TableBody />
        </Table>
    )
};

export default MainTable;